import { APP_THEME_COLOR } from '../../constants';

export default {
  addImage: { height: 40, width: 40 },
  addFormIcon: {
    position: 'absolute', right: 0, bottom: 0, backgroundColor: APP_THEME_COLOR, borderRadius: 50, marginRight: 30, marginBottom: 30, padding: 15,
  },
};
